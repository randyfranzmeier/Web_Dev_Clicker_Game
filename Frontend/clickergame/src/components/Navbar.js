import '../styles/Navbar.css';

export default function Navbar() {
    let navTitle = "Jello Clicker";
    return (
        <div>
            <nav>
                <h1>{navTitle}</h1>
            </nav>
        </div>
    )
}