import '../styles/Navbar.css';

export default function Navbar() {
    let navTitle = "Jello Clicker V2";
    return (
        <div>
            <nav>
                <h1>{navTitle}</h1>
            </nav>
        </div>
    )
}